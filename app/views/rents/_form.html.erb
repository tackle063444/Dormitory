<%= form_with(model: rent, local: true) do |form| %>
  <% if rent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rent.errors.count, "error") %> prohibited this rent from being saved:</h2>

      <ul>
      <% rent.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Room_id %>
    <%= form.collection_select :room_id, Room.all, :id, :room_num, class: 'form-control'  %>
  </div>

 <div class="field">
  <%= form.label :User_id %>
  <%= form.collection_select :user_id, User.all, :id, :user_fname, {class: 'form-control', multiple: true} %>
  <%= link_to 'Add User', '#', id: 'add-user-btn', class: 'btn btn-primary' %>
</div>

  <div class="field">
    <%= form.label :rent_start %>
    <%= form.date_select :rent_start, id: :rent_rent_start, class: 'form-control'  %>
  </div>

  <div class="field">
    <%= form.label :rent_end %>
    <%= form.date_select :rent_end, id: :rent_rent_end, class: 'form-control'  %>
  </div>

  <div class="field">
    <%= form.label :rent_history %>
    <%= form.text_field :rent_history, id: :rent_rent_history, class: 'form-control'  %>
  </div>

  <div class="actions">
    <%= form.submit "Confirm" , class: "btn btn-primary" %>
  </div>
<% end %>
